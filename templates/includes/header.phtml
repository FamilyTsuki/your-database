<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Inventaire</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="icon" type="image/png" href="favicon.png">
    <!-- Icônes pour écran d'accueil mobile -->
    <link rel="apple-touch-icon" href="apple-touch-icon.png">
    <link rel="icon" sizes="192x192" href="icon-192.png">
    

    
</head>
<body>
    <nav class="top-nav">
        <div class="div-modif-inv">
            <a href="index.php" class="nav-link">📦 Mes Bases</a>
            <?php 
            // Show action buttons only on database pages
            $current_page = basename($_SERVER['PHP_SELF']);
            // Show settings button on database-view.php for admins
            if (($current_page === 'database-view.php' || $current_page === 'database-ajouter.php' || $current_page === 'database-settings.php') && isset($_GET['id'])): 
                $db_id = $_GET['id'];
                // Check if admin (you might want to get this from session or pass it)
            ?>
                <a href="database-view.php?id=<?php echo $db_id; ?>" class="nav-action-btn btn-vewe">👀 afficher</a>
            <?php endif; ?>
            <?php 
            
            
            if (($current_page === 'database-view.php' || $current_page === 'database-ajouter.php' || $current_page === 'database-settings.php') && isset($_GET['id'])): 
                $db_id = $_GET['id'];
            ?>
                <a href="database-ajouter.php?id=<?php echo $db_id; ?>" class="nav-action-btn btn-add">➕ Ajouter</a>
            <?php endif; ?>
            
            <?php 
            // Show settings button on database-view.php for admins
            if (($current_page === 'database-view.php' || $current_page === 'database-ajouter.php' || $current_page === 'database-settings.php') && isset($_GET['id'])): 
                $db_id = $_GET['id'];
                // Check if admin (you might want to get this from session or pass it)
            ?>
                <a href="database-settings.php?id=<?php echo $db_id; ?>" class="nav-action-btn btn-settings">⚙️ Paramètres</a>
            <?php endif; ?>
            
        </div>
        <div class="user-info">
            <?php 
            $user = Auth::getUser();
            if ($user): 
            ?>
                <a href="profile.php" class="user-profile-link">
                    <?php if (!empty($user['profile_image'])): ?>
                        <img src="uploads/profiles/<?php echo htmlspecialchars($user['profile_image']); ?>" alt="Profile" class="nav-profile-img">
                    <?php else: ?>
                        <span class="nav-profile-icon">👤</span>
                    <?php endif; ?>
                    <span><?php echo htmlspecialchars($user['username']); ?></span>
                </a>
                <a href="logout.php" class="nav-action-btn" style="padding: 5px 10px; font-size: 0.8em; border: 1px solid var(--border);">Déconnexion</a>
            <?php endif; ?>
        </div>
    </nav>