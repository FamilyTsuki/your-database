<div class="container">
    <?php echo FlashMessage::render(); ?>
    
    <div class="dashboard-header">
        <h1>📦 Mes Bases de Données</h1>
        <button class="btn-primary" data-action="toggle-create">+ Créer une nouvelle base</button>
    </div>

    <!-- Formulaire de création de base -->
    <?php $csrf_token = CsrfToken::generate(); ?>
    <script>window.csrfToken = '<?php echo $csrf_token; ?>';</script>
    <div id="createForm" class="create-db-form" style="display:none;">
        <h2>Créer une nouvelle base de données</h2>
        <form method="POST" id="createDbForm">
            <div class="form-group">
                <label for="name">Nom de la base *</label>
                <input type="text" name="name" id="name" required placeholder="Ex: Garage, Cuisine, Bureau..." maxlength="25">
            </div>
            
            <div class="form-group">
                <label for="description">Description</label>
                <textarea name="description" id="description" placeholder="Décrivez le contenu de cette base..." maxlength="255" rows="3"></textarea>
            </div>
            
            <input type="hidden" name="csrf_token" value="<?php echo $csrf_token; ?>">
                <div class="form-actions">
                <button type="submit" class="btn-primary">Créer</button>
                <button type="button" class="btn-secondary" data-action="toggle-create">Annuler</button>
            </div>
        </form>
    </div>
    <!-- Liste des bases de données (remplie côté client) -->
    <div class="databases-grid" id="databasesGrid">
        <div class="loading">Chargement…</div>
    </div>
</div>

<!-- toggleCreate handled in public/js/app.js -->
